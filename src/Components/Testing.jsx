import React from 'react'

function npm install --save react-background-slideshow() {
  return (
    <div>npm install --save react-background-slideshow</div>
  )
}

export default npm install --save react-background-slideshow